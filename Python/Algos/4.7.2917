function secLarge(arr) {
  var max = arr[0];

  for(var i = 1; i < arr.length; i++){
    if(max < arr[i]){
      max = arr[i];
    }
  }
    for(var i = 0; i<arr.length; i++){
      if(max != arr[0]){
        var maxtwo = arr[0];

      }
      else {
        var maxtwo = arr[1];
      }
      if(max > arr[i] && maxtwo < arr[i]){
        maxtwo = arr[i];
      }
    }

  console.log(max);
  console.log(maxtwo);
}

secLarge([-9,-8,-3,-8,-5,-2]);
